/**
 * Per-course enrollment refresh from classes.berkeley.edu.
 * Flip to `false` to disable the class-page refresh button and GraphQL mutation.
 * Backend spaces outbound catalog requests at most 1 per 3 seconds.
 */
export const ENROLLMENT_CATALOG_REFRESH_ENABLED = true;
